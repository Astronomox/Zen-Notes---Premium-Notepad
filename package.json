{
  "name": "zen-notes---premium-notepad",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir dist && cp index.html index.tsx metadata.json dist/ && cp -r src dist/"
  },
  "devDependencies": {
    "@angular/build": "^21.0.0",
    "typescript": "^5.9.2"
  }
}